[phases.setup]
nixPkgs = ["python312", "gcc", "cmake", "libGL", "glib"]

[phases.install]
cmds = [
  "python -m venv venv",
  "source venv/bin/activate && pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = "source venv/bin/activate && gunicorn app:app --bind 0.0.0.0:5000"
